# https://docs.github.com/en/code-security/dependabot/working-with-dependabot/automating-dependabot-with-github-actions#automatically-approving-a-pull-request
env:
    CI: 1

jobs:
    dependabot:
        if: github.event.pull_request.user.login == 'dependabot[bot]'
        runs-on: ubuntu-24.04

        steps:
            - id: metadata
              name: Fetch the Dependabot metadata
              uses: dependabot/fetch-metadata@ffa630c65fa7e0ecfa0625b5ceda64399aea1b36
              with:
                  github-token: "${{ secrets.GITHUB_TOKEN }}"

            - name: Checkout
              uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
              with:
                  ref: ${{ github.head_ref }}

            - name: Cache pnpm store
              uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae
              with:
                  key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
                  path: ~/.local/share/pnpm/store
                  restore-keys: ${{ runner.os }}-pnpm-

            - name: Install pnpm
              uses: pnpm/action-setup@078e9d416474b29c0c387560859308974f7e9c53
              with:
                  version: latest

            # https://docs.astral.sh/uv/guides/integration/github/
            - name: Install uv
              uses: astral-sh/setup-uv@cec208311dfd045dd5311c1add060b2062131d57
              with:
                  enable-cache: true

            # https://just.systems/man/en/github-actions.html
            - name: Install Just
              uses: extractions/setup-just@53165ef7e734c5c07cb06b3c8e7b647c5aa16db3

            - name: Cache Cargo
              uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae
              with:
                  key: ${{ runner.os }}-cargo-${{ hashFiles('rust/Cargo.lock') }}
                  path: |
                      ~/.cargo/registry/index/
                      ~/.cargo/registry/cache/
                      ~/.cargo/git/db/
                      rust/target/
                  restore-keys: ${{ runner.os }}-cargo-

            - name: Cache NuGet
              uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
              with:
                  key: ${{ runner.os }}-nuget-${{ hashFiles('csharp/**/*.csproj') }}
                  path: ~/.nuget/packages
                  restore-keys: ${{ runner.os }}-nuget-

            - name: Install Linux libraries
              run: sudo apt-get install -y cpanminus

            - name: Install dependencies
              run: just install

            - name: Migrate any code artifacts to newer versions
              run: just migrate

            - name: Commit migrations
              run: |
                  git config user.email "github-actions[bot]@users.noreply.github.com"
                  git config user.name "github-actions[bot]"
                  git add --all
                  git diff --staged --quiet || git commit -m "chore: apply automated migrations"
                  git push

            - name: Spellcheck
              run: just spellcheck

            - name: C# tests
              run: just csharp

            - name: C++ tests
              run: just cpp

            - name: JavaScript tests
              run: just javascript

            - name: Perl tests
              run: just perl

            - name: Python tests
              run: just python

            - name: Rust tests
              run: just rust

            - env:
                  GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
                  PR_URL: ${{github.event.pull_request.html_url}}
              name: Approve the Dependabot PR
              run: gh pr review --approve "$PR_URL"

            - env:
                  GH_TOKEN: ${{secrets.GITHUB_TOKEN}}
                  PR_URL: ${{github.event.pull_request.html_url}}
              name: Merge and delete the branch
              run: |
                  if [[ "${{ steps.metadata.outputs.dependency-type }}" == "direct:development" ]]; then
                      scope="deps-dev"
                  else
                      scope="deps"
                  fi
                  subject="chore(${scope}): bump ${{ steps.metadata.outputs.dependency-names }} from ${{ steps.metadata.outputs.previous-version }} to ${{ steps.metadata.outputs.new-version }}"
                  gh pr merge --squash --delete-branch --subject "$subject" "$PR_URL"

name: Dependabot auto-approve
on: pull_request

permissions:
    contents: write
    pull-requests: write
